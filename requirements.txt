# Core dependencies
flet==0.83.0

# HTTP requests for providers (with SOCKS proxy support)
requests[socks]>=2.28.0

# HTML parsing
beautifulsoup4>=4.11.0
lxml>=4.9.0

# Windows jump list management
comtypes>=1.2.0

# Windows icon management
pywin32>=305

# Torrent functionality
# NOTE: libtorrent installation on Windows:
# Use one of these methods:
# 1. conda install -c conda-forge python-libtorrent
# 2. Download pre-built wheel from: https://github.com/arvidn/libtorrent/releases
# 3. pip install libtorrent-python (community build)
# libtorrent>=2.0.0

# System Tray
pystray>=0.19.5
Pillow>=10.0.0
